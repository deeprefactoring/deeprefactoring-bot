[Unit]
Description=DeepRefactoring bot

[Service]
User={{bot_user}}
Group={{bot_group}}
Type=simple
WorkingDirectory={{bot_root}}
ExecStart={{bot_binary}}
KillMode=process
Restart=on-failure


[Install]
WantedBy=multi-user.target
