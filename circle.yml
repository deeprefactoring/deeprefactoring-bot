machine:
  timezone:
    UTC
  services:
    - docker

dependencies:
  override:
    - echo pass

test:
  override:
    - make ci
  post:
    # upload artifacts
    - mkdir -p $CIRCLE_TEST_REPORTS/{junit,coverage}
    - cp coverage.html $CIRCLE_TEST_REPORTS/coverage/
    - cp deeprefactoring-bot $CIRCLE_ARTIFACTS/
