package main

import "fmt"

func main() {
	// If message.new_chat_members
	if true {
		// member.name = message.new_chat_members
		member := "<username>"
		fmt.Printf("Вечер в хату, %s!\n", member)
	}
}
